# Copiá este archivo a ".env" (mismo directorio que el docker-compose.yml)
#   cp .env.example .env
# y completá con tu private key de WireGuard de ProtonVPN.
# NO subas el .env a git ni a ningún lado público.

WIREGUARD_PRIVATE_KEY=pegá_acá_tu_private_key_de_proton
